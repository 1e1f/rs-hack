{"t":1777323039,"type":"scan","id":"R024-T1","changes":{"assignee":{"after":"agent:claude"},"gotchas":{"after":["Spawn handle types: tauri::async_runtime::spawn returns tauri::async_runtime::JoinHandle, NOT tokio::task::JoinHandle. The two have similar names but distinct types — using the wrong import gives an opaque E0308 mismatch at the spawn site. RigEntry::bridge and event_bridge::spawn_for must keep the tauri import."]},"handoff":{"after":["Multi-rig AppState landed. New types in app/tauri/src/state.rs: RigId (blake3 of canonicalized path, 'rig:hex12' form, deterministic), Rig (id/name/path/kind), RigKind (Local/Remote), RigEntry (rig + Arc<KgService> + bridge JoinHandle), RigsFile (rigs[], lastActive, camelCase JSON via serde rename). AppState replaced its single Arc<KgService> with rigs: Arc<RwLock<HashMap<RigId, RigEntry>>> + active: Arc<RwLock<Option<RigId>>>; methods attach_rig (idempotent via path-derived id), detach_rig (aborts bridge + clears active if matching), list_rigs, set_active, active_id, active_svc (compat accessor for arch_* commands). Persistence: rigs_file_path() defaults ~/.yah/rigs.json (override YAH_HOME for tests), load_rigs_file/save_rigs_file. event_bridge::spawn replaced with spawn_for(rig_id, svc, app_handle) — emits a flattened RigEvent { rig_id, ...event } payload via Tauri events so the renderer can route by rig. lib.rs setup() now: AppState::empty → manage → spawn boot_registry which reattaches every persisted rig (no auto-boot, indexing is lazy on user click), restores lastActive, then handles YAH_RIG_ROOT (attach + activate + auto-boot+watch) for dev. commands.rs: every arch_* command routes through active(&state) -> Arc<KgService> which returns 'no active rig — attach one via the rig selector' if none. Tests: 3 unit tests in state::tests (RigId determinism, RigId differs across paths, RigsFile round-trips through camelCase JSON). cargo build -p yah-tauri green; full workspace builds clean (only the pre-existing 42 dead-code warnings on yah Ticket methods)."]},"next_steps":{"before":["Replace AppState.svc: Arc<KgService> with rigs: Arc<RwLock<HashMap<RigId, Arc<KgService>>>> + active: Arc<RwLock<Option<RigId>>>","Each KgService gets its own watcher task; per-rig event channel multiplexed onto a single bridge that tags every ArchEvent with rig_id","Persist rig list to ~/.yah/rigs.json ({rigs: [{id, name, path, kind: 'local'}], lastActive: id}); load on bootstrap()"],"after":["Replace AppState.svc: Arc<KgService> with rigs: Arc<RwLock<HashMap<RigId, Arc<KgService>>>> + active: Arc<RwLock<Option<RigId>>>","Each KgService gets its own watcher task; per-rig event channel multiplexed onto a single bridge that tags every ArchEvent with rig_id","Persist rig list to ~/.yah/rigs.json ({rigs: [{id, name, path, kind: 'local'}], lastActive: id}); load on bootstrap()","R024-T2 (Tauri rig-management commands): wire list_rigs/attach_rig/detach_rig/set_active_rig as #[tauri::command]s on top of the AppState methods that now exist; saving the registry should call state.snapshot_to_file().await + save_rigs_file() after each mutation. Also add rig_id: RigId arg to every arch_* command and route via state.svc_for(&id) instead of active(&state).","Renderer (yah-ui) listens to 'arch:event' and now needs to read the rig_id field from the wrapped payload — that's the breaking change for the env adapter when the Tauri target ships."]},"status":{"before":"open","after":"review"}}}
{"t":1777323039,"type":"scan","id":"R024-T2","changes":{"assignee":{"after":"agent:claude"},"status":{"before":"open","after":"in-progress"}}}
{"t":1777323039,"type":"disappeared","id":"R024-T3","lastTicket":{"id":"R024-T3","title":"Attention badge: per-rig handoff-count surfaced in RigSelector menu","item_type":"ticket","status":"open","parent":"R024","next_steps":["Extend Rig interface (yah-ui/src/types.ts): add needsAttention?: number","Derive count in App.tsx: tickets where status === 'handoff' grouped by rigId (later: also Col01 smell hits)","RigSelector menu row shows a brass pill with the count when > 0; main toolbar dot gets an inset oxblood pip when any rig has attention > 0","Mock data: seed mockRigs with 3 local entries + varying needsAttention values to exercise the UI before backend lands"],"verify":["cd yah-ui && bun run typecheck && bun run build","cd yah-ui && bun run dev  # toggle between rigs in selector; confirm badge renders"],"depends_on":[],"see_also":[],"file":"yah/src/main.rs","line":306,"target":{"Module":{"path":"main"}},"files":[{"path":"yah/src/main.rs","line":306}]}}
{"t":1777323487,"type":"scan","id":"R024-T2","changes":{"handoff":{"after":["Rig-management surface landed. Four new commands: rig_list -> Vec<RigDto>, rig_attach(path, name) -> RigDto, rig_detach(rig_id) -> bool, rig_set_active(rig_id) -> bool. Each mutation snapshots the registry and writes it through state::save_rigs_file (warning logged on I/O failure; never aborts the command). Every existing arch_* command now takes a rig_id: RigId param and routes via state.svc_for(&id) (was: active(&state)). arch_open_rig drops its rig_root: String param — path is now looked up from the rig entry via state.path_for, since attach precedes open in the new flow. Renderer side: yah-ui's `Rig` interface in src/types.ts is a subset of RigDto (missing path/lastActiveAt) — those fields are forward-compatible. Frontend integration is mock-driven via R024-T3 and isn't blocked."]},"next_steps":{"before":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts"],"after":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts","yah-ui: pass rig_id alongside every arch.* invoke once env adapter switches to the Tauri target (R024-T3 stays mock-driven for now)","Extend yah-ui Rig interface to include path + lastActiveAt to consume the new fields"]},"status":{"before":"in-progress","after":"review"}}}
{"t":1777325218,"type":"scan","id":"R024","changes":{"status":{"before":"open","after":"in-progress"}}}
{"t":1777325447,"type":"scan","id":"R024","changes":{"status":{"before":"in-progress","after":"open"}}}
{"t":1777325447,"type":"scan","id":"R024-T2","changes":{"handoff":{"before":["Rig-management surface landed. Four new commands: rig_list -> Vec<RigDto>, rig_attach(path, name) -> RigDto, rig_detach(rig_id) -> bool, rig_set_active(rig_id) -> bool. Each mutation snapshots the registry and writes it through state::save_rigs_file (warning logged on I/O failure; never aborts the command). Every existing arch_* command now takes a rig_id: RigId param and routes via state.svc_for(&id) (was: active(&state)). arch_open_rig drops its rig_root: String param — path is now looked up from the rig entry via state.path_for, since attach precedes open in the new flow. Renderer side: yah-ui's `Rig` interface in src/types.ts is a subset of RigDto (missing path/lastActiveAt) — those fields are forward-compatible. Frontend integration is mock-driven via R024-T3 and isn't blocked."]},"next_steps":{"before":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts","yah-ui: pass rig_id alongside every arch.* invoke once env adapter switches to the Tauri target (R024-T3 stays mock-driven for now)","Extend yah-ui Rig interface to include path + lastActiveAt to consume the new fields"],"after":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts"]},"status":{"before":"review","after":"in-progress"}}}
{"t":1777325449,"type":"scan","id":"R024","changes":{"status":{"before":"open","after":"in-progress"}}}
{"t":1777325449,"type":"scan","id":"R024-T2","changes":{"handoff":{"after":["Rig-management surface landed. Four new commands: rig_list -> Vec<RigDto>, rig_attach(path, name) -> RigDto, rig_detach(rig_id) -> bool, rig_set_active(rig_id) -> bool. Each mutation snapshots the registry and writes it through state::save_rigs_file (warning logged on I/O failure; never aborts the command). Every existing arch_* command now takes a rig_id: RigId param and routes via state.svc_for(&id) (was: active(&state)). arch_open_rig drops its rig_root: String param — path is now looked up from the rig entry via state.path_for, since attach precedes open in the new flow. Renderer side: yah-ui's `Rig` interface in src/types.ts is a subset of RigDto (missing path/lastActiveAt) — those fields are forward-compatible. Frontend integration is mock-driven via R024-T3 and isn't blocked."]},"next_steps":{"before":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts"],"after":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts","yah-ui: pass rig_id alongside every arch.* invoke once env adapter switches to the Tauri target (R024-T3 stays mock-driven for now)","Extend yah-ui Rig interface to include path + lastActiveAt to consume the new fields"]},"status":{"before":"in-progress","after":"review"}}}
{"t":1777325560,"type":"scan","id":"R024-T3","ticket":{"id":"R024-T3","title":"Attention badge: per-rig handoff-count surfaced in RigSelector menu","item_type":"ticket","status":"review","assignee":"agent:claude","parent":"R024","handoff":["yah-ui side landed: Rig interface (src/types.ts) gained needsAttention?: number, path?: string, lastActiveAt?: number — all forward-compatible with RigDto here. mockRigs (src/mock.ts) seeded with 3 local entries (synth-engine, rs-hack, yah-design) at varying needsAttention values 0/1/2 + lastActiveAt staircase to exercise the recency + attention axes. App.tsx derives needsAttention via useMemo: handoff-ticket count is attributed to the active rigId today (tickets don't carry rigId yet) and falls back to the seeded mock value for non-active rigs. RigSelector.tsx grew an AttentionPill (brass pill, count, font-mono 10px) rendered on each menu row when count > 0, plus a RigDot pip prop that lays an oxblood dot on the toolbar status dot whenever any rig has attention. Local-rig sub-line now shows path?? 'local filesystem' so the three-rig demo is visually distinct. bun run typecheck and bun run build are clean (only the pre-existing serve.ts errors tracked under R015-T6 remain)."],"next_steps":["Once tickets carry rig_id from the daemon, replace App.tsx's active-rig-only attribution with a true Object.groupBy(tickets, t => t.rigId)","Col01 smell hits (Active>1, mixed-children zones) should also feed the attention count alongside handoff"],"verify":["cd yah-ui && bun run typecheck","cd yah-ui && bun run build"],"gotchas":["yah scanner is Rust-only and reads //! module-level / /// on top-level items only — this ticket lives on RigDto because that's the wire shape the renderer mirrors. The original R024-T3 disappeared because its annotation was placed inside an #[arg(...)] attribute on an enum variant, which the syn-based scanner cannot see (see memory: feedback_annotation_placement)."],"depends_on":[],"see_also":[],"file":"app/tauri/src/state.rs","line":132,"target":{"Struct":{"name":"RigDto","module":"state"}},"files":[{"path":"app/tauri/src/state.rs","line":132}]}}
{"t":1777325586,"type":"scan","id":"R024","changes":{"status":{"before":"in-progress","after":"review"}}}
{"t":1777325714,"type":"scan","id":"R024-T3","changes":{"gotchas":{"before":["yah scanner is Rust-only and reads //! module-level / /// on top-level items only — this ticket lives on RigDto because that's the wire shape the renderer mirrors. The original R024-T3 disappeared because its annotation was placed inside an #[arg(...)] attribute on an enum variant, which the syn-based scanner cannot see (see memory: feedback_annotation_placement)."],"after":["yah scanner is Rust-only and reads //! module-level / /// on top-level items only — this ticket lives on RigDto because that's the wire shape the renderer mirrors. The original R024-T3 disappeared because its annotation was placed inside an #[arg(...)] attribute on an enum variant, which the syn-based scanner cannot see (see memory: feedback_annotation_placement).","yah-ui typecheck currently fails on env/index.ts:154 — mod.makeKv is referenced but tauri.ts doesn't export it yet. That gap is owned by R015-F3 (KVStore wrapper, in-progress). T3's bun run build is clean and T3 didn't touch env/index.ts; treat the makeKv error as out-of-scope at review."]}}}
{"t":1777327547,"type":"scan","id":"R024-T3","changes":{"gotchas":{"before":["yah scanner is Rust-only and reads //! module-level / /// on top-level items only — this ticket lives on RigDto because that's the wire shape the renderer mirrors. The original R024-T3 disappeared because its annotation was placed inside an #[arg(...)] attribute on an enum variant, which the syn-based scanner cannot see (see memory: feedback_annotation_placement).","yah-ui typecheck currently fails on env/index.ts:154 — mod.makeKv is referenced but tauri.ts doesn't export it yet. That gap is owned by R015-F3 (KVStore wrapper, in-progress). T3's bun run build is clean and T3 didn't touch env/index.ts; treat the makeKv error as out-of-scope at review."],"after":["yah scanner is Rust-only and reads //! module-level / /// on top-level items only — this ticket lives on RigDto because that's the wire shape the renderer mirrors. The original R024-T3 disappeared because its annotation was placed inside an #[arg(...)] attribute on an enum variant, which the syn-based scanner cannot see (see memory: feedback_annotation_placement).","rigList() is fetched once on mount — runtime attach (when the menu items are wired) needs to setRigs() too, or move that fetch into a refresh callback the attach flow can invoke after success."]},"handoff":{"before":["yah-ui side landed: Rig interface (src/types.ts) gained needsAttention?: number, path?: string, lastActiveAt?: number — all forward-compatible with RigDto here. mockRigs (src/mock.ts) seeded with 3 local entries (synth-engine, rs-hack, yah-design) at varying needsAttention values 0/1/2 + lastActiveAt staircase to exercise the recency + attention axes. App.tsx derives needsAttention via useMemo: handoff-ticket count is attributed to the active rigId today (tickets don't carry rigId yet) and falls back to the seeded mock value for non-active rigs. RigSelector.tsx grew an AttentionPill (brass pill, count, font-mono 10px) rendered on each menu row when count > 0, plus a RigDot pip prop that lays an oxblood dot on the toolbar status dot whenever any rig has attention. Local-rig sub-line now shows path?? 'local filesystem' so the three-rig demo is visually distinct. bun run typecheck and bun run build are clean (only the pre-existing serve.ts errors tracked under R015-T6 remain)."],"after":["Selector now lives end-to-end on T2's backend. Pass 1: Rig interface (src/types.ts) gained needsAttention?, path?, lastActiveAt?; RigSelector grew AttentionPill (brass) on menu rows + an oxblood pip on the title-bar dot when any rig has attention. Pass 2 (smoke-test fix): App.tsx no longer hard-codes mockRigs — rigs are state seeded from rpc.rigList() on mount (last-active wins as the initial pick), and the Board fetch effect now keys on rigId so picking a different rig retargets immediately. rpc gained rigSetActive(rigId) (Rpc interface in env/index.ts; tauri.ts invokes 'rig_set_active'; browser.ts returns false). The rigId effect best-effort calls rigSetActive then refetches listTickets/listRelays — failures are swallowed for the mock-id dev path. mockRigs stays as the browser-only seed for component inspection. Verified: cd yah-ui && bun run typecheck (only the pre-existing serve.ts errors under R015-T6); bun run build (1672 modules, 3.67MB); cargo build -p yah-tauri."]},"next_steps":{"before":["Once tickets carry rig_id from the daemon, replace App.tsx's active-rig-only attribution with a true Object.groupBy(tickets, t => t.rigId)","Col01 smell hits (Active>1, mixed-children zones) should also feed the attention count alongside handoff"],"after":["Attach-rig flow: 'Open local folder…' / 'Connect remote rig…' menu items still inert — wire to rpc.rigAttach + refresh state.rigs after success","Once tickets carry rig_id from the daemon, replace App.tsx's active-rig-only attribution with a true Object.groupBy(tickets, t => t.rigId)","Col01 smell hits (Active>1, mixed-children zones) should also feed the attention count alongside handoff"]},"verify":{"before":["cd yah-ui && bun run typecheck","cd yah-ui && bun run build"],"after":["cd yah-ui && bun run typecheck","cd yah-ui && bun run build","YAH_RIG_ROOT=$(pwd) cargo run -p yah-tauri  # selector should show the rig's name (not 'synth-engine'); flipping to a mock rig should retarget the Board"]}}}
{"t":1777327710,"type":"scan","id":"R024","ticket":{"id":"R024","title":"Multi-rig Tauri host: registry, attention badges, rig-management commands","item_type":"relay","status":"review","assignee":"agent:claude","parent":"R013","handoff":["Sprint scope expansion to support 3+ local rigs day-one with per-rig attention badges in RigSelector. Sibling to R017 (KG features) and R018 (pi-mono agent). Builds on R016 watcher/event-bridge wiring."],"next_steps":["Land T1 (multi-rig AppState) before T2/T3 — both depend on the rig-keyed state shape","T2 may go in parallel with T3 once T1 lands: T2 is Rust-only (Tauri commands), T3 is yah-ui-only (mock-driven)"],"depends_on":[],"see_also":[],"file":"app/tauri/src/state.rs","line":25,"target":{"Module":{"path":"state"}},"files":[{"path":"app/tauri/src/state.rs","line":25}]}}
{"t":1777327710,"type":"scan","id":"R024-T1","ticket":{"id":"R024-T1","title":"Multi-rig AppState: HashMap<RigId, KgService> + per-rig watchers + ~/.yah/rigs.json persistence","item_type":"ticket","status":"review","assignee":"agent:claude","parent":"R024","handoff":["Multi-rig AppState landed. New types in app/tauri/src/state.rs: RigId (blake3 of canonicalized path, 'rig:hex12' form, deterministic), Rig (id/name/path/kind), RigKind (Local/Remote), RigEntry (rig + Arc<KgService> + bridge JoinHandle), RigsFile (rigs[], lastActive, camelCase JSON via serde rename). AppState replaced its single Arc<KgService> with rigs: Arc<RwLock<HashMap<RigId, RigEntry>>> + active: Arc<RwLock<Option<RigId>>>; methods attach_rig (idempotent via path-derived id), detach_rig (aborts bridge + clears active if matching), list_rigs, set_active, active_id, active_svc (compat accessor for arch_* commands). Persistence: rigs_file_path() defaults ~/.yah/rigs.json (override YAH_HOME for tests), load_rigs_file/save_rigs_file. event_bridge::spawn replaced with spawn_for(rig_id, svc, app_handle) — emits a flattened RigEvent { rig_id, ...event } payload via Tauri events so the renderer can route by rig. lib.rs setup() now: AppState::empty → manage → spawn boot_registry which reattaches every persisted rig (no auto-boot, indexing is lazy on user click), restores lastActive, then handles YAH_RIG_ROOT (attach + activate + auto-boot+watch) for dev. commands.rs: every arch_* command routes through active(&state) -> Arc<KgService> which returns 'no active rig — attach one via the rig selector' if none. Tests: 3 unit tests in state::tests (RigId determinism, RigId differs across paths, RigsFile round-trips through camelCase JSON). cargo build -p yah-tauri green; full workspace builds clean (only the pre-existing 42 dead-code warnings on yah Ticket methods)."],"next_steps":["Replace AppState.svc: Arc<KgService> with rigs: Arc<RwLock<HashMap<RigId, Arc<KgService>>>> + active: Arc<RwLock<Option<RigId>>>","Each KgService gets its own watcher task; per-rig event channel multiplexed onto a single bridge that tags every ArchEvent with rig_id","Persist rig list to ~/.yah/rigs.json ({rigs: [{id, name, path, kind: 'local'}], lastActive: id}); load on bootstrap()","R024-T2 (Tauri rig-management commands): wire list_rigs/attach_rig/detach_rig/set_active_rig as #[tauri::command]s on top of the AppState methods that now exist; saving the registry should call state.snapshot_to_file().await + save_rigs_file() after each mutation. Also add rig_id: RigId arg to every arch_* command and route via state.svc_for(&id) instead of active(&state).","Renderer (yah-ui) listens to 'arch:event' and now needs to read the rig_id field from the wrapped payload — that's the breaking change for the env adapter when the Tauri target ships."],"verify":["cargo build -p yah-tauri"],"gotchas":["Spawn handle types: tauri::async_runtime::spawn returns tauri::async_runtime::JoinHandle, NOT tokio::task::JoinHandle. The two have similar names but distinct types — using the wrong import gives an opaque E0308 mismatch at the spawn site. RigEntry::bridge and event_bridge::spawn_for must keep the tauri import."],"depends_on":[],"see_also":["architecture/yah-ui-implementation-guide.md"],"file":"app/tauri/src/state.rs","line":33,"target":{"Module":{"path":"state"}},"files":[{"path":"app/tauri/src/state.rs","line":33}]}}
{"t":1777327710,"type":"scan","id":"R024-T2","ticket":{"id":"R024-T2","title":"Tauri rig-management commands + rig_id parameter on every arch_* command","item_type":"ticket","status":"review","assignee":"agent:claude","parent":"R024","handoff":["Rig-management surface landed. Four new commands: rig_list -> Vec<RigDto>, rig_attach(path, name) -> RigDto, rig_detach(rig_id) -> bool, rig_set_active(rig_id) -> bool. Each mutation snapshots the registry and writes it through state::save_rigs_file (warning logged on I/O failure; never aborts the command). Every existing arch_* command now takes a rig_id: RigId param and routes via state.svc_for(&id) (was: active(&state)). arch_open_rig drops its rig_root: String param — path is now looked up from the rig entry via state.path_for, since attach precedes open in the new flow. Renderer side: yah-ui's `Rig` interface in src/types.ts is a subset of RigDto (missing path/lastActiveAt) — those fields are forward-compatible. Frontend integration is mock-driven via R024-T3 and isn't blocked."],"next_steps":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts","yah-ui: pass rig_id alongside every arch.* invoke once env adapter switches to the Tauri target (R024-T3 stays mock-driven for now)","Extend yah-ui Rig interface to include path + lastActiveAt to consume the new fields"],"verify":["cargo build -p yah-tauri"],"depends_on":[],"see_also":[],"file":"app/tauri/src/commands.rs","line":32,"target":{"Module":{"path":"commands"}},"files":[{"path":"app/tauri/src/commands.rs","line":32}]}}
{"t":1777338850,"type":"scan","id":"R024","changes":{"status":{"before":"review","after":"open"}}}
{"t":1777338850,"type":"scan","id":"R024-T2","changes":{"handoff":{"before":["Rig-management surface landed. Four new commands: rig_list -> Vec<RigDto>, rig_attach(path, name) -> RigDto, rig_detach(rig_id) -> bool, rig_set_active(rig_id) -> bool. Each mutation snapshots the registry and writes it through state::save_rigs_file (warning logged on I/O failure; never aborts the command). Every existing arch_* command now takes a rig_id: RigId param and routes via state.svc_for(&id) (was: active(&state)). arch_open_rig drops its rig_root: String param — path is now looked up from the rig entry via state.path_for, since attach precedes open in the new flow. Renderer side: yah-ui's `Rig` interface in src/types.ts is a subset of RigDto (missing path/lastActiveAt) — those fields are forward-compatible. Frontend integration is mock-driven via R024-T3 and isn't blocked."]},"next_steps":{"before":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts","yah-ui: pass rig_id alongside every arch.* invoke once env adapter switches to the Tauri target (R024-T3 stays mock-driven for now)","Extend yah-ui Rig interface to include path + lastActiveAt to consume the new fields"],"after":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts"]},"status":{"before":"review","after":"in-progress"}}}
{"t":1777338850,"type":"disappeared","id":"R024-T3","lastTicket":{"id":"R024-T3","title":"Attention badge: per-rig handoff-count surfaced in RigSelector menu","item_type":"ticket","status":"review","assignee":"agent:claude","parent":"R024","handoff":["Selector now lives end-to-end on T2's backend. Pass 1: Rig interface (src/types.ts) gained needsAttention?, path?, lastActiveAt?; RigSelector grew AttentionPill (brass) on menu rows + an oxblood pip on the title-bar dot when any rig has attention. Pass 2 (smoke-test fix): App.tsx no longer hard-codes mockRigs — rigs are state seeded from rpc.rigList() on mount (last-active wins as the initial pick), and the Board fetch effect now keys on rigId so picking a different rig retargets immediately. rpc gained rigSetActive(rigId) (Rpc interface in env/index.ts; tauri.ts invokes 'rig_set_active'; browser.ts returns false). The rigId effect best-effort calls rigSetActive then refetches listTickets/listRelays — failures are swallowed for the mock-id dev path. mockRigs stays as the browser-only seed for component inspection. Verified: cd yah-ui && bun run typecheck (only the pre-existing serve.ts errors under R015-T6); bun run build (1672 modules, 3.67MB); cargo build -p yah-tauri."],"next_steps":["Attach-rig flow: 'Open local folder…' / 'Connect remote rig…' menu items still inert — wire to rpc.rigAttach + refresh state.rigs after success","Once tickets carry rig_id from the daemon, replace App.tsx's active-rig-only attribution with a true Object.groupBy(tickets, t => t.rigId)","Col01 smell hits (Active>1, mixed-children zones) should also feed the attention count alongside handoff"],"verify":["cd yah-ui && bun run typecheck","cd yah-ui && bun run build","YAH_RIG_ROOT=$(pwd) cargo run -p yah-tauri  # selector should show the rig's name (not 'synth-engine'); flipping to a mock rig should retarget the Board"],"gotchas":["yah scanner is Rust-only and reads //! module-level / /// on top-level items only — this ticket lives on RigDto because that's the wire shape the renderer mirrors. The original R024-T3 disappeared because its annotation was placed inside an #[arg(...)] attribute on an enum variant, which the syn-based scanner cannot see (see memory: feedback_annotation_placement).","rigList() is fetched once on mount — runtime attach (when the menu items are wired) needs to setRigs() too, or move that fetch into a refresh callback the attach flow can invoke after success."],"depends_on":[],"see_also":[],"file":"app/tauri/src/state.rs","line":132,"target":{"Struct":{"name":"RigDto","module":"state"}},"files":[{"path":"app/tauri/src/state.rs","line":132}]}}
{"t":1777338852,"type":"scan","id":"R024","changes":{"status":{"before":"open","after":"review"}}}
{"t":1777338852,"type":"scan","id":"R024-T2","changes":{"handoff":{"after":["Rig-management surface landed. Four new commands: rig_list -> Vec<RigDto>, rig_attach(path, name) -> RigDto, rig_detach(rig_id) -> bool, rig_set_active(rig_id) -> bool. Each mutation snapshots the registry and writes it through state::save_rigs_file (warning logged on I/O failure; never aborts the command). Every existing arch_* command now takes a rig_id: RigId param and routes via state.svc_for(&id) (was: active(&state)). arch_open_rig drops its rig_root: String param — path is now looked up from the rig entry via state.path_for, since attach precedes open in the new flow. Renderer side: yah-ui's `Rig` interface in src/types.ts is a subset of RigDto (missing path/lastActiveAt) — those fields are forward-compatible. Frontend integration is mock-driven via R024-T3 and isn't blocked."]},"next_steps":{"before":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts"],"after":["New commands: list_rigs() -> Vec<RigDto>, attach_rig(path, name) -> RigDto, detach_rig(id), set_active_rig(id)","Add rig_id: RigId to every existing arch_* command param; route to the right KgService via AppState lookup","RigDto: { id, name, path, kind: 'local'|'remote', reachable, lastActiveAt }; matches yah-ui Rig interface in src/types.ts","yah-ui: pass rig_id alongside every arch.* invoke once env adapter switches to the Tauri target (R024-T3 stays mock-driven for now)","Extend yah-ui Rig interface to include path + lastActiveAt to consume the new fields"]},"status":{"before":"in-progress","after":"review"}}}
{"t":1777338852,"type":"scan","id":"R024-T3","ticket":{"id":"R024-T3","title":"Attention badge: per-rig handoff-count surfaced in RigSelector menu","item_type":"ticket","status":"review","assignee":"agent:claude","parent":"R024","handoff":["Selector now lives end-to-end on T2's backend. Pass 1: Rig interface (src/types.ts) gained needsAttention?, path?, lastActiveAt?; RigSelector grew AttentionPill (brass) on menu rows + an oxblood pip on the title-bar dot when any rig has attention. Pass 2 (smoke-test fix): App.tsx no longer hard-codes mockRigs — rigs are state seeded from rpc.rigList() on mount (last-active wins as the initial pick), and the Board fetch effect now keys on rigId so picking a different rig retargets immediately. rpc gained rigSetActive(rigId) (Rpc interface in env/index.ts; tauri.ts invokes 'rig_set_active'; browser.ts returns false). The rigId effect best-effort calls rigSetActive then refetches listTickets/listRelays — failures are swallowed for the mock-id dev path. mockRigs stays as the browser-only seed for component inspection. Verified: cd yah-ui && bun run typecheck (only the pre-existing serve.ts errors under R015-T6); bun run build (1672 modules, 3.67MB); cargo build -p yah-tauri."],"next_steps":["Attach-rig flow: 'Open local folder…' / 'Connect remote rig…' menu items still inert — wire to rpc.rigAttach + refresh state.rigs after success","Once tickets carry rig_id from the daemon, replace App.tsx's active-rig-only attribution with a true Object.groupBy(tickets, t => t.rigId)","Col01 smell hits (Active>1, mixed-children zones) should also feed the attention count alongside handoff"],"verify":["cd yah-ui && bun run typecheck","cd yah-ui && bun run build","YAH_RIG_ROOT=$(pwd) cargo run -p yah-tauri  # selector should show the rig's name (not 'synth-engine'); flipping to a mock rig should retarget the Board"],"gotchas":["yah scanner is Rust-only and reads //! module-level / /// on top-level items only — this ticket lives on RigDto because that's the wire shape the renderer mirrors. The original R024-T3 disappeared because its annotation was placed inside an #[arg(...)] attribute on an enum variant, which the syn-based scanner cannot see (see memory: feedback_annotation_placement).","rigList() is fetched once on mount — runtime attach (when the menu items are wired) needs to setRigs() too, or move that fetch into a refresh callback the attach flow can invoke after success."],"depends_on":[],"see_also":[],"file":"app/tauri/src/state.rs","line":138,"target":{"Struct":{"name":"RigDto","module":"state"}},"files":[{"path":"app/tauri/src/state.rs","line":138}]}}
