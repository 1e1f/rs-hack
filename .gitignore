/tmp
/src
target
node_modules
dist
/*.md
!README.md
!HUMAN.md
.claude
.DS_STORE
# Cargo.lock is committed at workspace root but ignored in sub-crates
Cargo.lock
!/Cargo.lock
# hack-board sibling-workspace registry — each clone owns its own view
.hack/worktrees.json