agents know how to move a ticket between columns "in progress and claimed can be collapsed to a single column"
add todo button (track in .hack / todo.md) get's UID
todo gets prompt copy button including hack-board usage info
file path printed at bottom of ticket, just use last folder path + filename, and copy button to full path
if todo has a "see" same treatment, name of file + copy to clipboard